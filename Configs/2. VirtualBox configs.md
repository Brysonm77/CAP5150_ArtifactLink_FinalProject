# All VMs had a secondary adapter enabled. It was attached to NAT and allowed for internect access.
Server (Ubuntu) Configs:
  * Base Memory = 4807 Mb
  * Processors = 4
  * Internal Network = LabVPN

Client (Windows) Configs:
  * Base Memory = 4096
  * Processors = 2
  * Internal Network = LabVPN
