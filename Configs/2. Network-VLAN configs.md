# All network configs will be held between 3 back ticks (`), the bullet points will be used to describe what commands were used to get there
## Ubuntu IP configs:
  * sudo nano /etc/netplan/lab.yaml
```
network: 
  version: 2
  renderer: networkd

ethernets:
 enp0s3:
  addresses: [10.10.10.1/24]
```
 * sudo netplan apply
 * ip -br a
