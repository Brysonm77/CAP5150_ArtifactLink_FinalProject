# All network configs will be held between {}, the bullet points will be used to describe what commands were used to get there
Ubuntu IP configs:
  * sudo nano /etc/netplan/lab.yaml
```
Network: 
  version: 2
  renderer: networkd
```
